const showAnswerButton = document.querySelector(".button");
const cardAnswer = document.querySelector(".Card-answer");

showAnswerButton.addEventListener("click", () => {
  // cardAnswer.style.visibility = "visible";
  console.log(cardAnswerButton.textContent);
});
